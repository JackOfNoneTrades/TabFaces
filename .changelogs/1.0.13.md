* When `trimTabMenu` is true, the column width will adapt to the length of the names, taking into account team names
* Added `trimTabMenuExtraWidth` to the config, allows to add extra space (for example to fit ping info)
* Added debug options to put all users into a team, to test previous features. Also added the `tabfaces_dbg_cleanup` that clears those teams afterwards
